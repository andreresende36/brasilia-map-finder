[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "npm run build:server"]

[start]
cmd = "NODE_ENV=production npm run start:prod"

